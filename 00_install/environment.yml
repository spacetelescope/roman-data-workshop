name: roman-data-workshop-env

dependencies:
  - jupyter
  - notebook >= 6.0
  - matplotlib >= 3.2
  - ipympl
  - numpy >= 1.20
  - packaging
  - pip
  - python == 3.11
  - pyyaml
  - pip:
      - asdf_standard
      - asdf
      - asdf-astropy
      - astropy >= 6.0
      - gwcs >= 0.20
      - stpipe
      - romancal >= 0.17.0
