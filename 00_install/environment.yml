name: roman-data-workshop-env

dependencies:
  - jupyter
  - notebook >= 6.0
  - matplotlib >= 3.2
  - numpy >= 1.20
  - packaging
  - pip
  - python >= 3.9
  - pyyaml
  - pip:
      - asdf
      - asdf-astropy
      - asdf-standard
      - astropy >= 5.0.4
      - gwcs >= 0.16
      - romancal >= 0.11
      - jwst >= 1.10.2
